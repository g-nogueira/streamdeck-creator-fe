export { GradientBuilder } from './gradientBuilder';

// Export public types that consumers might need
export type {
	GradientState,
	GradientStop,
	ColorInput,
	LinearDirection,
	RadialShape,
	RadialSize,
	RadialSizeKeyword, // Exporting the specific keyword type might be useful too
	RadialPosition
} from './types';